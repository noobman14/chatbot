# 🧠 前端面试知识大全 - 4天速成版

> 配合项目准备一起使用，确保基础扎实

---

## 📚 目录

1. [JavaScript 核心](#1-javascript-核心)
2. [TypeScript](#2-typescript)
3. [React 深入](#3-react-深入)
4. [CSS 与布局](#4-css-与布局)
5. [浏览器与网络](#5-浏览器与网络)
6. [性能优化](#6-性能优化)
7. [工程化与构建](#7-工程化与构建)
8. [手写代码题](#8-手写代码题)
9. [算法基础](#9-算法基础)
10. [二面/HR面准备](#10-二面hr面准备)

---

## 1. JavaScript 核心

### 1.1 数据类型 ⭐⭐⭐

```javascript
// 基本类型 (7种)
string, number, boolean, undefined, null, symbol, bigint

// 引用类型
Object, Array, Function, Date, RegExp...

// 类型判断
typeof null          // "object" (历史遗留bug)
typeof []            // "object"
Array.isArray([])    // true (推荐)
Object.prototype.toString.call([])  // "[object Array]"
```

**面试题**:
- `typeof` 和 `instanceof` 的区别？
- 如何准确判断一个变量的类型？
- `null` 和 `undefined` 的区别？

### 1.2 作用域与闭包 ⭐⭐⭐

```javascript
// 闭包 = 函数 + 其词法环境
function createCounter() {
  let count = 0;
  return function() {
    return ++count;
  }
}
const counter = createCounter();
counter(); // 1
counter(); // 2

// 经典面试题
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0); // 输出 3, 3, 3
}
// 解决方案1: let
for (let i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0); // 0, 1, 2
}
// 解决方案2: 闭包
for (var i = 0; i < 3; i++) {
  ((j) => setTimeout(() => console.log(j), 0))(i);
}
```

**面试题**:
- 什么是闭包？有什么应用场景？
- 闭包会导致内存泄漏吗？如何避免？

### 1.3 this 指向 ⭐⭐⭐

```javascript
// 规则优先级：new > bind/call/apply > obj.method > 默认

const obj = {
  name: 'Alice',
  sayName() { console.log(this.name); },
  sayNameArrow: () => { console.log(this.name); } // 箭头函数无自己的this
};

obj.sayName();      // Alice
obj.sayNameArrow(); // undefined (或window.name)

const fn = obj.sayName;
fn();               // undefined (严格模式报错)
fn.call({ name: 'Bob' }); // Bob
```

**面试题**:
- 箭头函数和普通函数的区别？
- call、apply、bind的区别？
- `new` 一个对象发生了什么？

### 1.4 原型链 ⭐⭐⭐

```javascript
// 每个对象都有 __proto__，指向其构造函数的 prototype
function Person(name) {
  this.name = name;
}
Person.prototype.sayHello = function() {
  console.log(`Hello, ${this.name}`);
};

const p = new Person('Alice');
p.sayHello(); // Hello, Alice

// 原型链
p.__proto__ === Person.prototype          // true
Person.prototype.__proto__ === Object.prototype  // true
Object.prototype.__proto__ === null       // true
```

**面试题**:
- 原型链是什么？画一下原型链图
- 如何实现继承？ES5和ES6的方式有什么区别？

### 1.5 Promise 与异步 ⭐⭐⭐

```javascript
// Promise 状态：pending → fulfilled / rejected（不可逆）

// 手写Promise.all
Promise.myAll = function(promises) {
  return new Promise((resolve, reject) => {
    const results = [];
    let count = 0;
    promises.forEach((p, i) => {
      Promise.resolve(p).then(res => {
        results[i] = res;
        if (++count === promises.length) resolve(results);
      }).catch(reject);
    });
  });
};

// async/await 本质是 Generator + 自动执行器
async function fetchData() {
  try {
    const res = await fetch('/api/data');
    return res.json();
  } catch (e) {
    console.error(e);
  }
}
```

**面试题**:
- Promise 有几种状态？状态转换规则？
- Promise.all、Promise.race、Promise.allSettled 区别？
- async/await 是如何实现的？
- 如何实现并发控制？（一次最多发N个请求）

### 1.6 事件循环 (Event Loop) ⭐⭐⭐

```javascript
console.log(1);
setTimeout(() => console.log(2), 0);
Promise.resolve().then(() => console.log(3));
console.log(4);
// 输出: 1, 4, 3, 2

// 宏任务: setTimeout, setInterval, I/O, UI渲染
// 微任务: Promise.then, MutationObserver, queueMicrotask

// 执行顺序: 同步代码 → 微任务队列 → 宏任务队列
```

**面试题**:
- 说说事件循环机制
- 宏任务和微任务区别？
- Node.js 的事件循环和浏览器有什么区别？

### 1.7 ES6+ 特性 ⭐⭐

```javascript
// 解构赋值
const { name, age = 18 } = user;
const [first, ...rest] = arr;

// 展开运算符
const merged = { ...obj1, ...obj2 };
const newArr = [...arr1, ...arr2];

// 可选链 & 空值合并
const name = user?.profile?.name ?? 'Anonymous';

// Proxy
const proxy = new Proxy(target, {
  get(obj, prop) { return obj[prop]; },
  set(obj, prop, value) { obj[prop] = value; return true; }
});
```

---

## 2. TypeScript

### 2.1 基础类型 ⭐⭐⭐

```typescript
// 基本类型
let name: string = 'Alice';
let age: number = 25;
let isActive: boolean = true;
let data: any = 'anything';
let unknown: unknown = 'safer than any';

// 数组
let arr: number[] = [1, 2, 3];
let arr2: Array<number> = [1, 2, 3];

// 元组
let tuple: [string, number] = ['hello', 42];

// 联合类型
let value: string | number = 'hello';

// 字面量类型
let direction: 'left' | 'right' | 'up' | 'down' = 'left';
```

### 2.2 interface vs type ⭐⭐⭐

```typescript
// interface - 可扩展
interface User {
  name: string;
  age: number;
}
interface User {
  email: string; // 声明合并
}

// type - 更灵活
type ID = string | number;
type Point = { x: number; y: number };
type Response<T> = { data: T; error: string | null };

// 何时用哪个？
// - interface: 定义对象形状，需要扩展时
// - type: 联合类型、交叉类型、工具类型
```

### 2.3 泛型 ⭐⭐⭐

```typescript
// 泛型函数
function identity<T>(arg: T): T {
  return arg;
}

// 泛型接口
interface ApiResponse<T> {
  code: number;
  data: T;
  message: string;
}

// 泛型约束
function getLength<T extends { length: number }>(arg: T): number {
  return arg.length;
}

// 你的项目中的例子
async function handleResponse<T>(response: Response): Promise<T> {
  const data = await response.json();
  return data.data;
}
```

### 2.4 工具类型 ⭐⭐

```typescript
// 常用工具类型
Partial<T>      // 所有属性变可选
Required<T>     // 所有属性变必选
Readonly<T>     // 所有属性变只读
Pick<T, K>      // 选取部分属性
Omit<T, K>      // 排除部分属性
Record<K, V>    // 构建对象类型

// 示例
type UserUpdate = Partial<User>;
type UserPreview = Pick<User, 'name' | 'avatar'>;
```

**面试题**:
- interface 和 type 的区别？
- 什么是泛型？有什么使用场景？
- 如何实现一个 Omit 工具类型？

---

## 3. React 深入

### 3.1 Hooks 原理 ⭐⭐⭐

```javascript
// Hooks 规则
// 1. 只能在函数组件顶层调用
// 2. 不能在条件/循环中调用

// 为什么？—— Hooks 依赖调用顺序（链表存储）

// 简化版 useState 实现
let state = [];
let index = 0;
function useState(initialValue) {
  const currentIndex = index;
  state[currentIndex] = state[currentIndex] ?? initialValue;
  const setState = (newValue) => {
    state[currentIndex] = newValue;
    render(); // 触发重新渲染
  };
  index++;
  return [state[currentIndex], setState];
}
```

### 3.2 常用 Hooks 对比 ⭐⭐⭐

| Hook | 用途 | 何时使用 |
|------|------|----------|
| `useState` | 状态管理 | 组件内部状态 |
| `useEffect` | 副作用 | 数据获取、订阅、DOM操作 |
| `useLayoutEffect` | 同步副作用 | 需要在浏览器绘制前执行 |
| `useMemo` | 缓存计算结果 | 避免昂贵的重复计算 |
| `useCallback` | 缓存函数引用 | 传递给子组件的回调 |
| `useRef` | 持久化引用 | DOM引用、跨渲染保存值 |
| `useContext` | 跨层级传递 | 主题、用户信息等全局状态 |
| `useReducer` | 复杂状态逻辑 | 多个相关状态、复杂更新逻辑 |

### 3.3 性能优化 ⭐⭐⭐

```javascript
// 1. React.memo - 避免不必要的重新渲染
const MemoChild = React.memo(function Child({ data }) {
  return <div>{data}</div>;
});

// 2. useMemo - 缓存计算结果
const expensiveValue = useMemo(() => {
  return heavyComputation(a, b);
}, [a, b]);

// 3. useCallback - 缓存函数
const handleClick = useCallback(() => {
  doSomething(id);
}, [id]);

// 4. 懒加载
const LazyComponent = React.lazy(() => import('./Component'));
```

### 3.4 状态管理方案对比 ⭐⭐

| 方案 | 特点 | 适用场景 |
|------|------|----------|
| useState/useContext | 简单、无额外依赖 | 小型项目 |
| Redux | 可预测、DevTools | 大型复杂项目 |
| Zustand | 轻量、简洁API | 中型项目 |
| Jotai/Recoil | 原子化状态 | 细粒度状态管理 |
| React Query | 服务端状态 | API数据管理 |

### 3.5 React 19 新特性 ⭐⭐

```javascript
// 1. Actions - 简化异步操作
function UpdateName() {
  const [name, setName, isPending] = useActionState(
    async (prevName, formData) => {
      const newName = await updateNameInDB(formData.get('name'));
      return newName;
    },
    ''
  );
  return (
    <form action={setName}>
      <input name="name" />
      <button disabled={isPending}>Update</button>
    </form>
  );
}

// 2. use() - 读取Promise和Context
function Comments({ commentsPromise }) {
  const comments = use(commentsPromise); // 直接读取Promise
  return comments.map(c => <p>{c.text}</p>);
}

// 3. 自动批处理优化
// 4. Suspense改进
```

**面试题**:
- useEffect 和 useLayoutEffect 区别？
- React 如何进行性能优化？
- React Fiber 是什么？解决了什么问题？
- 虚拟DOM的原理？Diff算法？

---

## 4. CSS 与布局

### 4.1 盒模型 ⭐⭐⭐

```css
/* 标准盒模型: width = 内容宽度 */
box-sizing: content-box;

/* IE盒模型: width = 内容 + padding + border */
box-sizing: border-box; /* 推荐 */
```

### 4.2 Flexbox ⭐⭐⭐

```css
.container {
  display: flex;
  flex-direction: row;         /* 主轴方向 */
  justify-content: center;     /* 主轴对齐 */
  align-items: center;         /* 交叉轴对齐 */
  flex-wrap: wrap;            /* 换行 */
  gap: 10px;                  /* 间距 */
}

.item {
  flex: 1;                    /* flex-grow: 1, flex-shrink: 1, flex-basis: 0% */
  flex: 0 0 200px;            /* 固定宽度 */
}
```

### 4.3 Grid ⭐⭐

```css
.container {
  display: grid;
  grid-template-columns: repeat(3, 1fr);   /* 3列等分 */
  grid-template-columns: 200px 1fr 100px;  /* 固定+自适应 */
  grid-gap: 20px;
}

.item {
  grid-column: 1 / 3;   /* 跨两列 */
  grid-row: 1 / 2;
}
```

### 4.4 居中方案 ⭐⭐⭐

```css
/* 1. Flexbox (推荐) */
.parent { display: flex; justify-content: center; align-items: center; }

/* 2. Grid */
.parent { display: grid; place-items: center; }

/* 3. 绝对定位 + transform */
.child { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); }

/* 4. 绝对定位 + margin */
.child { position: absolute; top: 0; right: 0; bottom: 0; left: 0; margin: auto; width: 100px; height: 100px; }
```

### 4.5 响应式设计 ⭐⭐

```css
/* 媒体查询 */
@media (max-width: 768px) {
  .container { flex-direction: column; }
}

/* 移动优先 */
@media (min-width: 768px) { /* tablet */ }
@media (min-width: 1024px) { /* desktop */ }

/* 相对单位 */
rem    /* 相对于根元素 font-size */
em     /* 相对于父元素 font-size */
vw/vh  /* 视口宽度/高度的百分比 */
```

### 4.6 BFC ⭐⭐

```css
/* Block Formatting Context - 独立的渲染区域 */

/* 触发BFC */
overflow: hidden;
display: flex / grid / inline-block;
position: absolute / fixed;

/* BFC用途 */
/* 1. 清除浮动 */
/* 2. 防止margin重叠 */
/* 3. 阻止元素被浮动元素覆盖 */
```

**面试题**:
- 如何实现两栏布局？三栏布局？
- Flex布局和Grid布局区别？
- 如何实现一个三角形？
- CSS选择器优先级？

---

## 5. 浏览器与网络

### 5.1 HTTP ⭐⭐⭐

```
GET     获取资源 (幂等)
POST    创建资源
PUT     全量更新 (幂等)
PATCH   部分更新
DELETE  删除资源 (幂等)

常见状态码:
200 OK
201 Created
204 No Content
301 永久重定向
302 临时重定向
304 Not Modified (缓存)
400 Bad Request
401 Unauthorized
403 Forbidden
404 Not Found
500 Internal Server Error
502 Bad Gateway
503 Service Unavailable
```

### 5.2 HTTPS ⭐⭐

```
1. 客户端发起请求
2. 服务器返回证书 (公钥)
3. 客户端验证证书，生成随机密钥
4. 用公钥加密随机密钥发送给服务器
5. 服务器用私钥解密，得到随机密钥
6. 双方使用随机密钥进行对称加密通信
```

### 5.3 缓存 ⭐⭐⭐

```
强缓存:
Cache-Control: max-age=31536000  (优先级高)
Expires: Thu, 01 Dec 2024 00:00:00 GMT

协商缓存:
ETag / If-None-Match         (内容hash)
Last-Modified / If-Modified-Since  (时间戳)

缓存策略:
- HTML: no-cache (每次验证)
- JS/CSS: max-age + 文件hash
- 图片: max-age=31536000
```

### 5.4 跨域 (CORS) ⭐⭐⭐

```javascript
// 同源策略: 协议 + 域名 + 端口 必须相同

// 解决方案1: CORS (服务端设置)
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
Access-Control-Allow-Headers: Content-Type, Authorization

// 解决方案2: 代理 (开发环境)
// vite.config.js
server: {
  proxy: {
    '/api': 'http://localhost:8080'
  }
}

// 解决方案3: JSONP (仅GET，已过时)
```

### 5.5 浏览器渲染过程 ⭐⭐⭐

```
1. 解析HTML → DOM树
2. 解析CSS → CSSOM树
3. 合并 → Render树
4. 布局 (Layout/Reflow) → 计算位置和大小
5. 绘制 (Paint) → 转换为像素
6. 合成 (Composite) → GPU渲染

重排触发: 改变尺寸、位置
重绘触发: 改变颜色、背景
Composite: transform, opacity (性能最好)
```

### 5.6 从URL输入到页面展示 ⭐⭐⭐

```
1. DNS解析 → 获取IP地址
2. TCP连接 → 三次握手
3. 发送HTTP请求
4. 服务器处理并返回响应
5. 浏览器解析HTML/CSS/JS
6. 渲染页面
7. TCP断开 → 四次挥手
```

### 5.7 Web安全 ⭐⭐

```javascript
// XSS (跨站脚本攻击)
// 攻击: 注入恶意脚本
// 防御: 转义输出、Content-Security-Policy

// CSRF (跨站请求伪造)
// 攻击: 利用用户已登录状态发送恶意请求
// 防御: CSRF Token、SameSite Cookie

// SQL注入
// 防御: 参数化查询、输入验证
```

---

## 6. 性能优化

### 6.1 加载优化 ⭐⭐⭐

```javascript
// 1. 代码分割
const LazyComponent = React.lazy(() => import('./Component'));

// 2. 图片优化
<img loading="lazy" src="..." />

// 3. 预加载关键资源
<link rel="preload" href="critical.css" as="style" />
<link rel="prefetch" href="next-page.js" as="script" />

// 4. 压缩
// gzip / brotli
// 树摇 (Tree Shaking)

// 5. CDN
```

### 6.2 运行时优化 ⭐⭐

```javascript
// 1. 虚拟滚动 (大量列表)
// 2. 防抖 / 节流
function debounce(fn, delay) {
  let timer;
  return (...args) => {
    clearTimeout(timer);
    timer = setTimeout(() => fn(...args), delay);
  };
}

function throttle(fn, delay) {
  let last = 0;
  return (...args) => {
    const now = Date.now();
    if (now - last >= delay) {
      last = now;
      fn(...args);
    }
  };
}

// 3. Web Worker (CPU密集型任务)
// 4. requestAnimationFrame (动画)
```

### 6.3 性能指标 ⭐⭐

```
Core Web Vitals:
- LCP (Largest Contentful Paint) < 2.5s  最大内容绘制
- FID (First Input Delay) < 100ms        首次输入延迟
- CLS (Cumulative Layout Shift) < 0.1    累计布局偏移

其他指标:
- FCP (First Contentful Paint)
- TTI (Time to Interactive)
- TTFB (Time to First Byte)
```

---

## 7. 工程化与构建

### 7.1 Vite ⭐⭐

```javascript
// 为什么比Webpack快？
// 1. 开发时使用原生ESM，无需打包
// 2. 按需编译 (只编译当前页面用到的模块)
// 3. 使用esbuild进行预构建 (比Babel快10-100倍)

// 配置
export default {
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom']
        }
      }
    }
  }
}
```

### 7.2 Webpack 基础 ⭐⭐

```javascript
// 核心概念
entry      // 入口
output     // 输出
loader     // 转换器 (处理非JS文件)
plugin     // 插件 (扩展功能)

// 常见loader
babel-loader, css-loader, style-loader, file-loader

// 常见plugin
HtmlWebpackPlugin, MiniCssExtractPlugin, DefinePlugin

// 优化
splitChunks, Tree Shaking, Source Map
```

### 7.3 Babel ⭐

```javascript
// 作用: 将新语法转换为旧语法

// 流程
源码 → Parse(解析) → AST → Transform(转换) → AST → Generate(生成) → 目标代码

// 常用preset
@babel/preset-env       // 根据目标环境转换
@babel/preset-react     // JSX转换
@babel/preset-typescript // TS转换
```

---

## 8. 手写代码题

### 8.1 经典手写题

```javascript
// 1. 防抖
function debounce(fn, delay) {
  let timer;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}

// 2. 节流
function throttle(fn, delay) {
  let last = 0;
  return function(...args) {
    const now = Date.now();
    if (now - last >= delay) {
      last = now;
      fn.apply(this, args);
    }
  };
}

// 3. 深拷贝
function deepClone(obj, map = new WeakMap()) {
  if (obj === null || typeof obj !== 'object') return obj;
  if (map.has(obj)) return map.get(obj);
  
  const clone = Array.isArray(obj) ? [] : {};
  map.set(obj, clone);
  
  for (const key of Object.keys(obj)) {
    clone[key] = deepClone(obj[key], map);
  }
  return clone;
}

// 4. 数组扁平化
function flatten(arr, depth = 1) {
  if (depth === 0) return arr.slice();
  return arr.reduce((acc, val) => 
    acc.concat(Array.isArray(val) ? flatten(val, depth - 1) : val), []);
}
// 或: arr.flat(Infinity)

// 5. 实现 new
function myNew(Constructor, ...args) {
  const obj = Object.create(Constructor.prototype);
  const result = Constructor.apply(obj, args);
  return result instanceof Object ? result : obj;
}

// 6. 实现 bind
Function.prototype.myBind = function(context, ...args) {
  const fn = this;
  return function(...innerArgs) {
    return fn.apply(context, [...args, ...innerArgs]);
  };
};

// 7. 实现 instanceof
function myInstanceof(obj, Constructor) {
  let proto = Object.getPrototypeOf(obj);
  while (proto) {
    if (proto === Constructor.prototype) return true;
    proto = Object.getPrototypeOf(proto);
  }
  return false;
}

// 8. Promise.all
Promise.myAll = function(promises) {
  return new Promise((resolve, reject) => {
    const results = [];
    let count = 0;
    promises.forEach((p, i) => {
      Promise.resolve(p).then(res => {
        results[i] = res;
        if (++count === promises.length) resolve(results);
      }).catch(reject);
    });
  });
};

// 9. 发布订阅模式
class EventEmitter {
  constructor() {
    this.events = {};
  }
  on(event, callback) {
    (this.events[event] ||= []).push(callback);
  }
  emit(event, ...args) {
    this.events[event]?.forEach(cb => cb(...args));
  }
  off(event, callback) {
    if (this.events[event]) {
      this.events[event] = this.events[event].filter(cb => cb !== callback);
    }
  }
}

// 10. LRU缓存
class LRUCache {
  constructor(capacity) {
    this.capacity = capacity;
    this.cache = new Map();
  }
  get(key) {
    if (!this.cache.has(key)) return -1;
    const value = this.cache.get(key);
    this.cache.delete(key);
    this.cache.set(key, value);
    return value;
  }
  put(key, value) {
    if (this.cache.has(key)) this.cache.delete(key);
    this.cache.set(key, value);
    if (this.cache.size > this.capacity) {
      this.cache.delete(this.cache.keys().next().value);
    }
  }
}
```

---

## 9. 算法基础

### 9.1 必会算法 ⭐⭐

```javascript
// 1. 排序
// 快速排序 O(nlogn)
function quickSort(arr) {
  if (arr.length <= 1) return arr;
  const pivot = arr[0];
  const left = arr.slice(1).filter(x => x < pivot);
  const right = arr.slice(1).filter(x => x >= pivot);
  return [...quickSort(left), pivot, ...quickSort(right)];
}

// 2. 二分查找 O(logn)
function binarySearch(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1;
}

// 3. 链表反转
function reverseList(head) {
  let prev = null, curr = head;
  while (curr) {
    const next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
  }
  return prev;
}

// 4. 有效括号
function isValid(s) {
  const stack = [];
  const map = { ')': '(', ']': '[', '}': '{' };
  for (const char of s) {
    if ('([{'.includes(char)) {
      stack.push(char);
    } else {
      if (stack.pop() !== map[char]) return false;
    }
  }
  return stack.length === 0;
}

// 5. 两数之和
function twoSum(nums, target) {
  const map = new Map();
  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    if (map.has(complement)) return [map.get(complement), i];
    map.set(nums[i], i);
  }
  return [];
}
```

---

## 10. 二面/HR面准备

### 10.1 常见问题

**自我介绍**
> 30秒-1分钟，突出：教育背景 + 技术栈 + 项目经验 + 求职意向

**为什么选择前端？**
> 对用户界面交互感兴趣 + 技术更新快有挑战 + 能直观看到成果

**职业规划**
> 短期：深耕技术，成为资深前端工程师
> 长期：技术专家方向 或 带团队方向

**你的优缺点**
> 优点：学习能力强 / 善于总结 / 注重代码质量
> 缺点：有时追求完美影响效率（正在学习权衡）

**有什么想问的？**
- 团队技术栈和工作流程？
- 新人培养机制？
- 这个岗位的主要职责和挑战？

### 10.2 项目介绍话术

**STAR法则**
- **S**ituation: 项目背景和目标
- **T**ask: 你负责的任务
- **A**ction: 你采取的行动和技术方案
- **R**esult: 取得的成果（最好量化）

---

## 📋 复习优先级

| 优先级 | 知识点 | 用时建议 |
|--------|--------|----------|
| ⭐⭐⭐ | JS核心(闭包/this/原型链/Promise/事件循环) | 3小时 |
| ⭐⭐⭐ | React Hooks原理与优化 | 2小时 |
| ⭐⭐⭐ | CSS布局(Flex/居中) | 1小时 |
| ⭐⭐⭐ | HTTP/缓存/跨域 | 1.5小时 |
| ⭐⭐⭐ | 手写代码(防抖/深拷贝/Promise.all) | 2小时 |
| ⭐⭐ | TypeScript泛型/工具类型 | 1小时 |
| ⭐⭐ | 浏览器渲染/性能优化 | 1小时 |
| ⭐⭐ | Vite/工程化 | 0.5小时 |
| ⭐ | 算法基础 | 1小时 |

---

祝面试顺利！💪
